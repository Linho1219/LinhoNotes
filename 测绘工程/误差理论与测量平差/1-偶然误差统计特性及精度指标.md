# 1 偶然误差的统计特性及精度指标

## 回顾正态分布

- 随机变量的**分布函数、概率密度和分布律**都能完整地描述随机变量
- 由随机变量的分布所确定的，能刻画随机变量某一方面的特征的常数称为**数字特征**

### 一维正态分布

$$
f(x)=\frac1{\sqrt{2\pi}\sigma}\exp\left[-\frac{(x-\mu)^2}{2\sigma^2} \right]
$$

### 二维正态分布

$$
f(x,y)=\frac1{2\pi\sigma_x\sigma_y\sqrt{1-\rho^2}}\exp\left\{
-\frac1{2(1-\rho^2)}\left[
\frac{(x-\mu_x)^2}{\sigma_x^2}
-2\rho\frac{(x-\mu_x)(x-\mu_y)}{\sigma_x\sigma_y}
+\frac{(y-\mu_y)^2}{\sigma_y^2}
\right]
\right\}
$$

其中 $\rho$ 为相关系数，$\rho=0$ 时 $f(x,y)=f(x)f(y)$。

对于正态分布，不相关 = 相互独立

### $n$ 维正态分布的矩阵形式

$$
f(X_1,X_2,\cdots,X_n)=\frac1{(2\pi)^{\frac n2}|\boldsymbol D_{XX}|^{\frac12}}
\exp\left\{-\frac12
[\boldsymbol X-E(\boldsymbol X)]^{\mathrm T}
\boldsymbol D_{XX}^{-1}
[\boldsymbol X-E(\boldsymbol X)]
\right\}
$$

其中有

$$
\mathop{\boldsymbol X}_{n\times1}=\begin{bmatrix}
X_1\\X_2\\\vdots\\X_n
\end{bmatrix},
\quad
\mathop{E(\boldsymbol X)}_{n\times1}=\begin{bmatrix}
E(X_1)\\E(X_2)\\\vdots\\E(X_n)
\end{bmatrix},
\quad
\mathop{\boldsymbol D_{XX}}_{n\times n}=
\begin{bmatrix}
\sigma_{X_1}^2 & \sigma_{X_1X_2} & \cdots & \sigma_{X_1X_n} \\
\sigma_{X_1X_2} & \sigma_{X_2}^2 & \cdots & \sigma_{X_2X_n} \\
\vdots & \vdots & \ddots & \vdots \\
\sigma_{X_1X_n} & \sigma_{X_2X_n} & \cdots & \sigma_{X_n}^2 \\
\end{bmatrix},
$$

## 偶然误差的统计特性

- 有界性：绝对值不会超过一定阈值
- 趋向性：大误差概率小，小误差概率大
- 对称性：正负误差频率大致相等
- 抵偿性：观测次数无限增大时理论平均值趋于零

## 精度的概念

- **精度**：观测值 $X$ 与期望 $E(X)$ 之差
- **准确度**：期望 $E(X)$ 与真值 $\widetilde X$ 之差
  - 衡量标准：偏差 $\varepsilon=\widetilde X-E(X)$
- **精准度**：观测值 $X$ 与真值 $\widetilde X$ 之差
  - 衡量标准：均方误差 $\operatorname {MSE}(X)=E(X-\widetilde X)^2$

> [!note]
>
> ![](./images/precision-example.svg)
>
> | 图  | 精度 | 准确度 | 精准度 |
> | :-: | :--: | :----: | :----: |
> | (a) |  低  |   高   |   低   |
> | (b) |  高  |   低   |   低   |
> | (c) |  高  |   高   |   高   |

## 衡量精度的指标

本课程后续介绍的内容一般不考虑系统误差，即认为 $E(X)=\widetilde X$。

### 方差和中误差

定义**中误差**（即标准差）

$$
\sigma^2=D(X)=E\left\{[X-E(X)]^2\right\}=\int_{-\infty}^{+\infty}[x-E(X)]^2f(x)\mathrm dx
$$

考虑误差 $\varDelta=E(X)-X$，则有

$$
\sigma^2=E(\varDelta^2)=\lim_{n\to\infty}\frac1n\sum\varDelta_i^2
$$

当观测值 $n$ 有限时，有中误差的估计值 $\hat\sigma$

$$
\hat\sigma^2=\frac1n\sum\varDelta^2
$$

### 平均误差

$$
\theta=E(|\varDelta|)=\lim_{n\to\infty}\frac1n\sum|\varDelta_i|
$$

当观测值 $n$ 有限时，有平均误差的估计值 $\hat\theta$

$$
\hat\theta=\frac1n\sum|\varDelta_i|
$$

平均误差与中误差的关系

$$
\begin{align}
\theta&=E(|\varDelta|) \\
&=\int_{-\infty}^{+\infty}|\varDelta|f(\varDelta)\mathrm d\varDelta \\
&=2\int_0^{+\infty}\frac\varDelta{\sqrt{2\pi}\sigma}\exp\left(-\frac{\varDelta^2}{2\sigma^2} \right)\mathrm d\varDelta \\
&=\frac2{\sqrt{2\pi}}\int_0^{+\infty}-\sigma\mathrm d\exp\left(-\frac{\varDelta^2}{2\sigma^2} \right) \\
&=\frac{2\sigma}{\sqrt{2\pi}}\left.\exp\left(-\frac{\varDelta^2}{2\sigma^2} \right) \right|_0^{+\infty} \\
&=\sqrt{\frac2\pi}\sigma
\end{align}
$$

故有

$$
\left\{\begin{align}
\theta&=\sqrt{\frac2\pi}\sigma\approx0.797\,9\sigma\approx\frac45\sigma \\
\sigma&=\sqrt{\frac\pi2}\theta\approx1.253\,3\theta\approx\frac54\theta
\end{align} \right.
$$

### 或然误差

定义或然误差 $\rho$ 为观测误差 $\varDelta$ 在 $(-\rho,+\rho)$ 内的概率为 $\dfrac12$。

由于 $\varDelta\sim N(0,\sigma^2)$，将其标准化为 $\eta=\dfrac\varDelta\sigma\sim N(0,1)$，有 $\varPhi\left( \dfrac\rho\sigma\right)-\varPhi\left(-\dfrac\rho\sigma\right)=\dfrac12$，得 $\dfrac\rho\sigma-0.647\,5$，故有

$$
\left\{\begin{align}
\rho&=0.647\,5\sigma\approx\frac23\sigma \\
\sigma&=1.482\,6\rho\approx\frac32\rho
\end{align} \right.
$$

### 精度的衡量

$\sigma,\theta,\rho$ 三者都可作为衡量精度的指标。由于 $n$ 有限，只能求得估值，与理论值有差异。
如果 $n$ 很小，求得的估值都不可靠。**对于同一组观测值，中误差能更灵敏地反映大误差的影响，且中误差具有明确的几何意义。**

**因此通常使用中误差衡量精度。**

通常取 3 倍（少数取 2 倍）中误差作为偶然误差 $\varDelta$ 的限差。

### 相对误差

$\sigma,\theta,\rho$ 等称为绝对误差。

观测值的中误差与观测值之比称为**相对中误差**。

$$
\frac{\sigma_S}S=\frac1{S/\sigma_S}=\frac1N
$$

通常化为 $\dfrac1{整百数}$ 的形式。

相对中误差 对应 相对极限误差。

> [!tip]
>
> - 观测误差的精度**等于**观测值的精度
>
>   观测误差和观测值为加减常数的关系，离散程度是相同的
>
> - 有系统误差**不会**引起观测值观测误差的精度变化
>
>   系统误差为加减常数，不改变离散程度
