# 6 统计量及其分布

## 统计量

对于样本的函数 $h(X_1,X_2,\cdots,X_n)$，只要不包含位置参数，均称为**统计量**。

- 样本均值 $\bar X=\dfrac1n\sum\limits_{i=1}^nX_i$
- 样本 $k$ 阶原点矩 $A_k=\dfrac1n\sum\limits_{i=1}^nX_i^k$，$k=1,2,\cdots$
- 样本 $k$ 阶中心矩 $B_k=\dfrac1n\sum\limits_{i=1}^n(X_i-\bar X)^k$
- ==样本 $2$ 阶中心矩== $S_n^2=\dfrac1{\color{orange}n}\sum\limits_{i=1}^n(X_i-\bar X)^k$
- ==样本方差== $S^2=\dfrac1{\color{orange}{n-1}}\sum\limits_{i=1}^n(X_i-\bar X)^2$

**定理** 对于任意常数 $c$ 有

$$
\sum_{i=1}^n(X_i-\bar X)^2=\sum_{i=1}^n(X_i-c)^2-n(\bar X-c)^2
$$

> [!tip]
>
> $c$ 的常用候选：$0$，$E(X)$

**定理** 设 $(X_1,\cdots,X_n)$ 是取自总体 $X$ 的一个样本，若 $E(X^2)<+\infty$，则

$$
\begin{gathered}
E(\bar X)=E(X),\quad \operatorname {Var}(\bar X)=\frac{\operatorname {Var}(x)}n\\
E(S_n^2)={\color{orange}\frac{n-1}n}\operatorname {Var}(x),\quad
E(S^2)=\operatorname {Var}(X)
\end{gathered}
$$

**定理** 当 $n\to\infty$ 有

$$
\bar X\xrightarrow PE(X),\quad
S^2\xrightarrow P\operatorname{Var}(X),\quad
S_n^2\xrightarrow P\operatorname{Var}(X),\quad
$$

## 常用分布

### $\chi^2$ 分布

**定义** 设 $X_1,X_2,\cdots,X_n$ 为独立同分布随机变量，有 $X_i\sim N(0,1)$。则定义随机变量 $U=\sum\limits_{i=1}^nX_i^2$ 服从自由度为 $n$ 的 $\chi^2$ 分布，记为 $U\sim\chi^2(n)$。

$\chi^2$ 分布的密度函数和分布函数均不能用初等函数表示。

- 期望 $E(X)=n$
- 方差 $\operatorname {Var}(X)=2n$

特别低，自由度为 $2$ 的 $\chi^2$ 分布就是参数为 $\dfrac12$ 的 [指数分布](./3-连续型随机变量#指数分布)，即 $E\left(\dfrac12\right)=\chi^2(2)$。

<<< ./images/chi-squared.ggb#suite

### t 分布（学生分布）

**定义** 设随机变量 $X\sim N(0,1),Y\sim\chi^2(n)$ 且 $X,Y$ 相互独立，称随机变量 $T=\dfrac{X}{\sqrt{Y/n}}$ 所服从的分布为自由度 $n$ 的 t 分布，记为 $T\sim t(n)$。

当 $n\ge30$ 时，t 分布可使用标准正态分布近似替代。

期望 $E(X)=0$。

<<< ./images/student.ggb#suite

### F 分布

**定义** 设随机变量 $U\sim\chi^2(m),V\sim\chi^2(n)$ 且 $U,V$ 相互独立，则称随机变量 $F=\dfrac{U/m}{V/n}$ 服从自由度为 $(m,n)$ 的 F 分布，记为 $F\sim F(m,n)$。

<<< ./images/F.ggb#suite
